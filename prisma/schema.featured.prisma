
model Orders {
  id    Int     @default(autoincrement()) @id  
  user_id Int
  product_id Int
  quantity Int
  created_at Date  
}

model Products {
  id    Int     @default(autoincrement()) @id  
  name String
  price Float
  category String
}